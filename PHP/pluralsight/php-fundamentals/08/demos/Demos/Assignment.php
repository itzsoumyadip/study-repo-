<?php

// --------------------------------
// -- Assignment Operator ~ +=, -=, %=, *=, /=, **=
// --------------------------------

/*

Using the same varialbe and performing the same mathmetical operations stores the value back to the same variable.

*/

$usingSameVar = 5;

//$usingSameVar = $usingSameVar + 1;
//$usingSameVar++;

//$usingSameVar += 3;
//$usingSameVar -= 3;
//$usingSameVar *= 3;
//$usingSameVar /= 3;
//$usingSameVar %= 3;
$usingSameVar **= 3;

echo $usingSameVar;

